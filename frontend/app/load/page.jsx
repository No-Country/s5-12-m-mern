import React from "react";
import {Loader} from "../components/loaders/genericloader"

//page load solo para testear como se visualiza, se puede borrar la carpeta completa, el componente Loading es el importante
const Load = () => {
    return (
        <Loader/>
    )
}

export default Load